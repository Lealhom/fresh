<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hy.manager.domain.business.ProductMapper">
	<insert id="insert" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
		insert into t_product
		(name,status,createTime,updateTime,fileId,brandId,description)
		values
		(#{name},#{status},#{createTime},#{updateTime},#{fileId},#{brandId},#{description})
	</insert>
	<insert id="addCategoryId">
		insert into t_category_product 
		(productId,categoryId)
		values
		(#{productId},#{categoryId})
	</insert>
	<update id="update" parameterType="Product">
		update t_product
		<set>
			name=#{name},
			description=#{description}
		</set>
		where id = #{id}
	</update>
	
	<delete id="deleteById" parameterType="int">
		delete from t_product where id=#{id}
	</delete>
	
	<delete id="deleteByIds">
		delete from t_product where id in
		<foreach collection="ids" item="id" open="(" close=")" separator=",">
		#{id}
		</foreach>
	</delete>
	
	<select id="selectById" resultType="Product">
		select * from t_product where id=#{id}
	</select>
	
	<sql id="gridWhere">
		from t_product u
		<where>
		</where>
	</sql>
	<select id="listPaged" resultType="Product">
		select t1.id,t1.name,t1.description,t1.status,t1.createTime,t1.updateTime,
			   t2.path filePath,t3.name	brandName
		from t_product t1 
		left join t_file t2 on t1.fileId = t2.id
		left join t_brand t3 on t1.brandId = t3.id
		<if test="rows > 0">
		limit #{start},#{rows}
		</if>
	</select>
	<select id="count" resultType="long">
		select count(*)
		<include refid="gridWhere"/>
	</select>
</mapper>